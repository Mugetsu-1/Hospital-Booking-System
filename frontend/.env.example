# Optional. Leave unset for local development: the Vite dev server proxies
# /api to http://localhost:5000 (see vite.config.js).
# Set this only when the production bundle is served from a different origin
# than the API, e.g. VITE_API_URL=https://api.example.com/api
# VITE_API_URL=/api
